import initial from './initial';
import scroll from './scroll';
import fromWorks from './fromWorks';
import toWorks from './toWorks';

const homeAnimations = {
    initial,
    scroll,
    fromWorks,
    toWorks,
};

export default homeAnimations;
